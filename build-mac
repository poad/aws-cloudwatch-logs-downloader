const builder = require('electron-builder');

builder.build({
    config: {
        'appId': 'local.test.aws-cloudwatch-logs-downloader',
        'mac':{
            'target': 'zip',
        }
    }
});
